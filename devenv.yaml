imports:
  - environments
  - environments/documentation
  - environments/rust
inputs:
  environments:
    flake: false
    url: git+ssh://git@codeberg.org/tymbalodeon/environments.git?dir=src
  project:
    flake: false
    url: path:.
  rust-overlay:
    inputs:
      nixpkgs:
        follows: nixpkgs
    url: github:oxalica/rust-overlay
